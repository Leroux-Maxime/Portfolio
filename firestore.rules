rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Collection des horaires: document id format: {userId}_{entryUid}
    match /horaires/{docId} {
      allow create: if request.auth != null
                    && docId.startsWith(request.auth.uid + '_')
                    && request.resource.data.userId == request.auth.uid;

      allow read, update, delete: if request.auth != null
                    && docId.startsWith(request.auth.uid + '_')
                    && resource.data.userId == request.auth.uid;
    }

    // Réglages utilisateur
    match /settings/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}

/*
Alternative temporaire (déverrouille tout pour les tests) — utiliser UNIQUEMENT pour debug:

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
*/
